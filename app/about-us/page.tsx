import { Metadata } from "next";

export const metadata: Metadata = {
  title: "About us",
};

const AboutUs = () => {
  return (
    <div>
      <h1>About us!</h1>
    </div>
  );
};

export default AboutUs;
